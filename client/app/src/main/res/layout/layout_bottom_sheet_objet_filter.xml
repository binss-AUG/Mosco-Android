<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/lg_surface_nav"
    android:paddingTop="@dimen/radius_lg">

    <!-- Drag Handle (Liquid Pill) -->
    <View
        android:id="@+id/v_drag_handle"
        android:layout_width="48dp"
        android:layout_height="5dp"
        android:background="@drawable/lg_btn_pill"
        android:backgroundTint="@color/lg_glass_stroke"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Header Row (Title & Reset) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_filter_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:layout_marginTop="@dimen/spacing_lg"
        app:layout_constraintTop_toBottomOf="@id/v_drag_handle">

        <TextView
            android:id="@+id/tv_filter_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/inventory_label_filter"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:fontFamily="@font/pretendard"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <TextView
            android:id="@+id/btn_filter_reset"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Reset"
            android:textColor="@color/lg_accent_primary"
            android:textSize="14sp"
            android:fontFamily="@font/pretendard"
            android:textStyle="bold"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Scrollable Content -->
    <ScrollView
        android:id="@+id/sv_filter_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/spacing_md"
        android:clipToPadding="false"
        android:paddingBottom="@dimen/spacing_xxl"
        android:scrollbars="none"
        app:layout_constraintTop_toBottomOf="@id/layout_filter_header"
        app:layout_constraintBottom_toTopOf="@id/layout_filter_actions">

        <LinearLayout
            android:id="@+id/ll_filter_sections_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/spacing_lg" />

    </ScrollView>

    <!-- Bottom Actions -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_filter_actions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:paddingTop="@dimen/spacing_md"
        android:paddingBottom="@dimen/spacing_lg"
        app:layout_constraintBottom_toBottomOf="parent">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_filter_apply"
            style="@style/LiquidGlass_Button_Primary"
            android:layout_width="match_parent"
            android:layout_height="@dimen/spacing_xxl"
            android:text="Apply Filter (0)"
            android:textAllCaps="false"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
