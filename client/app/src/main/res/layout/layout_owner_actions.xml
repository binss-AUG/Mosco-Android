<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/spacing_lg"
    android:gravity="center_horizontal">

    <!-- Group: Main Actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="@dimen/spacing_md">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_edit_profile"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="@dimen/spacing_xs"
            android:text="@string/profile_btn_edit"
            android:textColor="@color/white"
            app:strokeColor="@color/mosco_outline"
            app:cornerRadius="@dimen/radius_md"
            app:icon="@drawable/ic_edit_pen"
            app:iconTint="@color/white" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_settings"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/profile_btn_settings"
            android:textColor="@color/white"
            app:strokeColor="@color/mosco_outline"
            app:cornerRadius="@dimen/radius_md"
            app:icon="@drawable/ic_settings"
            app:iconTint="@color/white" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_resource_management"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/profile_btn_resource_mgmt"
        android:backgroundTint="@color/mosco_surface_container_high"
        android:textColor="@color/lg_accent_primary"
        app:cornerRadius="@dimen/radius_md"
        app:icon="@drawable/ic_items"
        app:iconTint="@color/lg_accent_primary" />

</LinearLayout>
