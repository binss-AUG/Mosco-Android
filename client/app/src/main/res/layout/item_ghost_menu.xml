<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/spacing_64dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/mosco_surface_alpha_20"
    app:cardCornerRadius="@dimen/radius_lg"
    app:cardElevation="0dp"
    app:strokeWidth="@dimen/stroke_thin"
    app:strokeColor="@color/mosco_outline_alpha_20">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/spacing_md">

        <!-- Cụm Text (Title + Desc) -->
        <LinearLayout
            android:id="@+id/ll_ghost_text_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/iv_ghost_arrow">

            <TextView
                android:id="@+id/tv_ghost_title"
                style="@style/MoscoText.Luxury.Card.Title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_body_sm"
                android:fontWeight="700"
                tools:text="GACHA SPIN" />

            <TextView
                android:id="@+id/tv_ghost_desc"
                style="@style/MoscoText.Luxury.Card.Desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Recycle cards or use tickets to get new ones" />

        </LinearLayout>

        <!-- Arrow Indicator (Căn phải) -->
        <ImageView
            android:id="@+id/iv_ghost_arrow"
            android:layout_width="@dimen/spacing_20dp"
            android:layout_height="@dimen/spacing_20dp"
            android:src="@drawable/ic_back_arrow"
            android:rotation="180"
            app:tint="@color/mosco_white_30"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>


