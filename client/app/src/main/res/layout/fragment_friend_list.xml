<?xml version="1.0" encoding="utf-8"?>
<!-- Fragment Friend List — Tích hợp Thống kê (Total/Online) — NO HARDCODE — CLEAN UI -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/transparent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_friend_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="16dp"
            android:paddingTop="8dp"
            android:clipToPadding="false" />

        <!-- Empty State -->
        <LinearLayout
            android:id="@+id/layout_friend_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">
            <com.airbnb.lottie.LottieAnimationView
                android:id="@+id/lottie_friend_empty"
                android:layout_width="140dp"
                android:layout_height="140dp"
                app:lottie_autoPlay="true"
                app:lottie_loop="true" />
            <TextView
                android:id="@+id/tv_friend_empty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/friend_empty_msg"
                style="@style/MoscoText.Luxury.Body"
                android:textColor="@color/white_40"
                android:textSize="13sp"
                android:layout_marginTop="8dp" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
