<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#990A0E14" /> <!-- Obsidian semi-transparent -->
    <corners android:radius="@dimen/spacing_100dp" /> <!-- Pill shape -->
    <stroke
        android:width="@dimen/stroke_thin"
        android:color="@color/white_10" />
</shape>

