<?xml version="1.0" encoding="utf-8"?>
<!-- Liquid Glass: Navigation bar surface (V1.1 Floating Style) -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Layer A: Light Absorption Base -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#E6080C14" />
            <corners android:radius="@dimen/lg_radius_nav" />
        </shape>
    </item>
    <!-- Layer B: Inner Glow (Subtle) -->
    <item android:top="1dp" android:bottom="1dp" android:left="1dp" android:right="1dp">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/lg_radius_nav" />
            <stroke android:width="1dp" android:color="#0DFFFFFF" />
        </shape>
    </item>
    <!-- Layer C: Top Highlight (Refraction) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/lg_radius_nav" />
            <gradient android:angle="270" 
                android:startColor="#1AFFFFFF" 
                android:centerColor="#00FFFFFF"
                android:endColor="#00FFFFFF" 
                android:centerY="0.3"
                android:type="linear" />
        </shape>
    </item>
    <!-- Layer D: Hairline Border (High Precision) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/lg_radius_nav" />
            <stroke android:width="@dimen/lg_stroke_hairline" android:color="@color/lg_glass_stroke_strong" />
        </shape>
    </item>
</layer-list>
