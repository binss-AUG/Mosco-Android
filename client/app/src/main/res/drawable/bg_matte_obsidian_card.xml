<?xml version="1.0" encoding="utf-8"?>
<!-- Liquid Glass: Matte panel card (Deep Space tone) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="@color/lg_background_mid"
        android:endColor="@color/lg_background"
        android:angle="225" />
    <corners android:radius="@dimen/lg_radius_panel" />
    <stroke
        android:width="@dimen/lg_stroke_hairline"
        android:color="@color/lg_glass_stroke_subtle" />
</shape>
