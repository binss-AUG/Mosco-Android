<?xml version="1.0" encoding="utf-8"?>
<!-- Liquid Glass: Deep space background (Layer 0) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Gradient tỏa tròn tạo chiều sâu không gian -->
    <gradient
        android:type="radial"
        android:gradientRadius="100%p"
        android:centerX="50%"
        android:centerY="40%"
        android:startColor="@color/lg_background_soft"
        android:centerColor="@color/lg_background_mid"
        android:endColor="@color/lg_background" />
</shape>
